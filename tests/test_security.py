import requests


def test_request_limit():
    print("OK")
    # query = "SELECT * WHERE {{ ?s ?p ?o }}"
    # url = f"http://141.57.8.18:8099/explanation?query_text={query}&language=en&shots=0&model=gpt-3.5-turbo"
    # r = requests.get(url)
    # assert r.status_code != 200